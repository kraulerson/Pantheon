# Bug Tracker

<!--
  This file tracks bugs found during UAT sessions and ad hoc testing.
  Status and severity patterns are read by scripts/test-gate.sh for phase gate checks.
  Do NOT change the table format — the column order and status values are parsed by scripts.
-->

> **Index policy (decision H1, 2026-07-09 — APPROVAL_LOG ruling):** this table is the
> canonical bug **index**; each row links to the UAT triage file holding the full
> evidence. Rows 1–7 backfilled from
> `tests/uat/sessions/2026-06-14-session-1/TRIAGE.md` (found by the Session 1
> adversarial sweep, fixed test-first the same session unless noted).

| # | Severity | Status | Feature | Description | Session | Disposition | Fix Reference | Verified In |
|---|---|---|---|---|---|---|---|---|
| 1 | SEV-2 | Fixed | devmachine-registry | Admin `PUT /api/dev-machines/:id` could forge `provisioned`/`sshKeyHandle` (invariant break); patch fields removed, dedicated `markProvisioned()` is the sole setter | Session 1 | Fix Now | 8caed24 | Session 1 |
| 2 | SEV-2 | Fixed | devmachine-registry | SSH `user`/`logicalName` regex accepted a leading `-` → ssh/ssh-copy-id option injection; leading dash now forbidden | Session 1 | Fix Now | 8caed24 | Session 1 |
| 3 | SEV-2 | Fixed | devmachine-registry | `KEY_HANDLE_RE` allowed `/` and `..` — traversal fail-open in the advertised custody guard; tightened to filename-safe grammar | Session 1 | Fix Now | 8caed24 | Session 1 |
| 4 | SEV-3 | Fixed | devmachine-registry | Editing host/port/user left stale `provisioned=true`; now resets provisioned state and clears the key handle | Session 1 | Fix Now | 8caed24 | Session 1 |
| 5 | SEV-3 | Fixed | devmachine-ssh-connection | Key custody `resolvePrivateKey` followed symlinks with a stat→read TOCTOU window; now `O_NOFOLLOW` + `fstat` on the same fd | Session 1 | Fix Now | 8caed24 | Session 1 |
| 6 | SEV-3 | Fixed | devmachine-ssh-connection | `storeKeyPair` umask window / loose dir mode; now `fchmod 0600` before writing bytes + key dir `0700` | Session 1 | Fix Now | 8caed24 | Session 1 |
| 7 | SEV-4 | Fixed | config-page | HTML forms posted urlencoded with no body parser registered (pre-existing, repo-wide); `@fastify/formbody` registered with the #9 auth work | Session 1 | Defer | bd5cb10 | — |
| 8 | SEV-3 | Fixed | control-plane (dev deps) | `vitest 3.2.4` pinned in `services/control-plane` matches GHSA-5xrq-8626-4rwp (critical-rated: file read/exec, but only when the Vitest **UI server** runs — we never run `--ui`, so dev-only exposure). Fixed 2026-08-17 with the remediation pass: vitest+coverage 3.2.7 exact-pinned in control-plane AND obsidian-mcp; `npm audit fix` cleared 5-week transitive backlog (@hono/node-server, body-parser, brace-expansion, esbuild); both services 0 vulnerabilities, 295+24 tests green | ad hoc (2026-07-09) | Fix Now | see 2026-08-17 remediation commit | — |
| 9 | SEV-3 | Fixed | deploy (install-debian.sh) | `npm ci` cannot install the committed `services/control-plane/package-lock.json` under **npm 10** — `@types/better-sqlite3` and `@types/ws` declare `@types/node: *`, which npm 10 re-resolves to the current latest (26.2.0 + undici-types 8.3.0) and then rejects the lockfile for missing those entries; npm 11 dedupes them to the locked 20.17.6 and installs clean. The installer pinned NodeSource `setup_20.x`, and Node 20 ships npm 10.8.2 → the deploy target could never build. Reproduced both ways on VM 1093 (npm 11.19.0 exit 0 / npm 10.8.2 exit 1). Fixed by raising the installer floor to Node 24 (ships npm 11) — which also lifts the host off a runtime that left security support 2026-04-30 (ADR-0002 amendment, ruling 2026-08-18). Note: `engines` still reads `>=20`, which remains true of the *runtime*; the npm ≥ 11 requirement is a lockfile property, not a runtime one | ad hoc (2026-08-18, skeleton step 1) | Fix Now | this commit | — |
| 10 | SEV-3 | Fixed | deploy (install-debian.sh) | Installer step 2 ran `npm ci && npm run build` as **root** (the script requires root for apt), leaving `~/.npm` root-owned for the service user; every later npm call by that user died `EACCES` on the cache, and the built `node_modules` were root-owned inside a user-owned checkout. Observed on VM 1093. Fixed: step 2 now drops to `$SERVICE_USER` via `sudo -u … env HOME=…` for both `npm ci` and `npm run build`, and repairs an already-root-owned `~/.npm` first so re-runs stay idempotent | ad hoc (2026-08-18, skeleton step 1) | Fix Now | this commit | — |
| 11 | SEV-3 | Fixed | deploy (install-debian.sh) | Same root cause as #10, different artifact: installer step 3 created the registry data dir (`services/control-plane/data`) as **root**, so the control-plane — which runs as `$SERVICE_USER` under the systemd units — could not create or write `control-plane.db` there. Caught on VM 1093 before first service start (dir was `root:root`, no db file). Fixed: step 3 now chowns `$DATA_DIR` to the service user alongside the existing `~/.pantheon` chown | ad hoc (2026-08-18, skeleton step 1) | Fix Now | this commit | — |
| 12 | SEV-2 | Fixed | config-page / devmachine-registry | **The Config page could not add a dev machine at all.** `POST /api/dev-machines` passed the raw body to `registry.createDevMachine(req.body as never)` with no edge parsing, but the page's own form is `application/x-www-form-urlencoded`, where every field is a STRING. `assertPort` requires a real number, so the form's `port=22` was rejected — and the message said "port out of range (1-65535): 22", which reads as though 22 were an illegal port. Reported by the operator against the live VM; reproduced three ways (urlencoded → 400, JSON number → 201, JSON string → 400). Fixed at the HTTP edge (`devMachineFormBody` in `src/http/app.ts`), which translates the form encoding only for urlencoded bodies and leaves JSON strict; the registry guards stay strict, with no coercion added to the domain. Error message now names the type as well as the range | ad hoc (2026-08-19, operator report) | Fix Now | this commit | — |
| 13 | SEV-3 | Fixed | config-page / devmachine-registry | Same root cause, quieter symptom: `enabled: input.enabled === true` measured against an HTML checkbox, which posts the string `"on"`. Had the port bug not blocked the form first, every machine added from the page would have been created **silently disabled** — no error, wrong state. Fixed in the same edge normalizer (`"on"`/`"true"` → true, absent → false) with a regression test | ad hoc (2026-08-19, found while fixing #12) | Fix Now | this commit | — |
| 14 | SEV-4 | Fixed | test-suite (gitea live integration) | `test/gitea-live.integration.test.ts` FAILS (not skips) since the 2026-08-17 token rotation: the new minimal-scope token is `write:repository,read:user` but the test needs `write:user` → `Gitea API 403`. The test is meant to be guarded — it runs whenever a token is present, so a correctly-scoped-down token turns a skip into a red suite. Unrelated to #12/#13; noted while verifying them. Fix by having the guard check scope (preferred) rather than widening the token and undoing the minimal-scope decision | ad hoc (2026-08-19) | Defer | UAT-2 remediation | — |
| 15 | SEV-3 | Fixed | control-plane (http) | The control-plane had **no `GET /` route**, so the bare origin answered a raw Fastify 404 (`{"message":"Route GET:/ not found"}`) — which is exactly what a bookmark or the chat UI's admin shortcut requests. The gap was invisible in every earlier check because those were made logged OUT, where the auth hook redirects to `/login` before routing happens; it only surfaces once authenticated. Reported by the operator clicking the new admin link. Fixed: `GET /` redirects to `/harness` (the same landing page the login flow already uses), left out of PUBLIC_PATHS so logged-out browsers still get `/login` and API callers still get 401 — both covered by regression tests | ad hoc (2026-08-19, operator report) | Fix Now | this commit | — |
| 16 | SEV-2 | Fixed | harness-frame | **Registering the first dev machine removed the only route to the Configuration page.** The `/admin/config` link existed solely inside the harness frame's zero-machines empty state (`shortcuts()`), so as soon as one machine existed the branch stopped rendering. With that machine unprovisioned there is also no terminal shortcut to open, leaving the operator on a page offering nothing but "+ New Session" and no way back to the page that provisions the machine. Reported by the operator immediately after registering his Mac mini. Fixed by moving Configuration into the page chrome (`<header>`), where it renders in every state; the empty-state sentence keeps its inline link. Four regression tests pin the link across empty / unprovisioned / ready registries and assert it lives in the header rather than the message | ad hoc (2026-08-19, operator report) | Fix Now | this commit | — |
| 17 | SEV-3 | Open | devmachine-enrollment / devmachine-connection | **No SSH host-key verification on either SSH path.** ssh2 is called without a `hostVerifier` and the harness keeps no known_hosts, so a host answering on the target's IP is trusted on sight. For the key-only terminal path the exposure is limited; for enrollment it is materially worse, because the FIRST connection carries the operator's machine password — an impersonating host on the LAN could harvest it. Found by the security audit for the UI-enrollment feature (2026-08-19) and documented inline in `enrollment-ssh.ts`; NOT introduced by it (the terminal path predates it). Fix: capture the host key at first enrollment, store it against the machine row, and verify it on every later connection (fail closed on mismatch) **2026-08-25 (tmux-aware launcher audit): a THIRD unverified path — `runRemoteCommand` (tmux listing) — same root cause; fix all three together (pin the host key at enrollment, pass `hostVerifier` on every ssh2 connect).** | ad hoc (2026-08-19, Build Loop security audit) | Defer | — | — |
| 18 | SEV-3 | Fixed | config-page (backends, service endpoints) | The BUGS #13 checkbox defect survives in two more places: `createBackend` and `createServiceEndpoint` both test `input.enabled === true` against an HTML checkbox that posts the string `"on"`, so **adding an AI Backend or a Service Endpoint from the Configuration page silently saves it disabled**. The #12/#13 fix normalized the dev-machine route only. Found 2026-08-19 while documenting the page for the user guide — the guide could not honestly describe the Enabled box as working. Fix: apply the same edge normalizer to those two routes (or lift it to a shared form-body helper) with the same regression tests | ad hoc (2026-08-19, user-guide accuracy pass) | Fix Now | UAT-2 remediation | — |
| 19 | SEV-2 | Fixed | config-page | **Every Edit and Remove button on the Configuration page is inert.** The page renders 7 `data-action` buttons (edit/remove for backends, service endpoints, dev machines, and remove for MCP servers) but ships **no JavaScript at all** (`grep -c "<script"` = 0, confirmed against the live page), so clicking any of them does nothing — no feedback, no error. Entries can be created from the page but never changed or deleted without calling the API directly. Found 2026-08-19 while documenting the page. Fix: either wire the buttons (fetch + confirm) or render them as real forms; until then the page over-promises what it can do | ad hoc (2026-08-19, user-guide accuracy pass) | Fix Now | UAT-2 remediation | — |
| 20 | SEV-3 | Fixed | harness-frame (operator auth UI) | **No logout control in the console UI (UAT session 2, scenario 4).** `POST /logout` exists and works (`operator-auth.ts:107-112` — destroys the session, clears the cookie, 302→/login) but the harness header exposes only + New Session / Configuration / Help, so the operator cannot end a session from the UI. Fix: add a logout form/button to the harness header (and the Configuration page header), emitted only when operator login is enabled | UAT Session 2 | Fix Now | UAT-2 remediation | — |
| 21 | SEV-3 | Fixed | config-page (all CRUD routes) | **Adding anything from the Configuration page dumps a raw-JSON page (UAT session 2, scenario 5).** All four CRUD POST handlers (`/api/backends` :208, `/api/service-endpoints` :226, `/api/dev-machines` :244, `/api/mcp-servers` :264) `reply.code(201)` and return the record, so a browser form submit lands on Firefox's JSON viewer; the row only appears after Back+refresh. Same family as BUGS #15. Fix: reuse the enrollment route content-type→303 redirect (`enrollment.ts:57-66`) on all four (JSON callers unchanged) | UAT Session 2 | Fix Now | UAT-2 remediation | — |
| 22 | SEV-2 | Fixed | harness-frame | **Terminal launch shortcut vanishes after one click; cannot open a second terminal from it (UAT session 2, scenario 15).** The `Claude CLI → <machine>` buttons render inside `<section data-welcome>`, and `switchTo(id)` sets `welcome.hidden = true` whenever a tab is open (`harness-frame.ts:51`), hiding every shortcut until the last tab closes. `+ New Session` (in the header) is unaffected — the operator's workaround. Fix: move the launch shortcuts into a persistent chrome element outside the hide-on-open welcome section | UAT Session 2 | Fix Now | UAT-2 remediation | — |
| 23 | SEV-3 | Fixed | test-suite (mcp-registration) | **A test that never tests its subject and is counted as a PASS.** `test/mcp-registration.test.ts`'s live test uses a bare `return` (tallied as passed, not skipped) and its happy path is `expect(true).toBe(true)` — it never runs the register→list→remove round-trip its name promises. Masks regressions in MCP registration and inflates the green count. Fix: use `ctx.skip()` for the unmet-precondition path and implement the real round-trip against the stubbed Peta client | automated-suite agent (2026-08-20) | Fix Now | UAT-2 remediation | — |
| 24 | SEV-4 | Open | config-page / peta | **MCP server registrations cannot be deleted (server-side).** The `/api/mcp-servers/:id` DELETE route is a documented no-op and no Peta `DELETE_SERVER` action is wired, so an MCP registration cannot be removed through the harness. Surfaced while fixing #19: the config-page MCP **Remove** button is now rendered **disabled** (with a tooltip) rather than lying. Fix: verify Peta's delete-server action code, add it to the Peta client + `McpRegistrationService.remove()`, wire the DELETE route, then re-enable the button | UAT Session 2 | Defer | — | — |
| 25 | SEV-3 | Open | harness-frame (tab shell) | **Tab close is unreachable by keyboard.** The ✕ is a `<span role="button">` nested inside the tab `<button>` with no `tabindex`/keydown handling; Enter/Space on the tab always selects, never closes — so a keyboard-only operator cannot close a tab, which is the only client path that closes the WebSocket / disposes the terminal. Pre-existing (tabbed-harness-shell); surfaced by the tmux-launcher audit. Fix: a sibling text-labelled `Close` button. | Audit 2026-08-25 (tmux-aware launcher) | Defer | — | — |
| 26 | SEV-3 | Open | terminal route (WebSocket) | **No `Origin` check on the terminal WebSocket upgrade** — only `SameSite=Lax` stands between a hostile page and a cookie-authed PTY. Pre-existing. Deferred because the household edge proxy rewrites `Host` to `192.168.1.93:443` (deploy/README), so a naive `Origin == Host` check would break the live terminal; needs the real header facts (whether `X-Forwarded-Host` can be trusted from the local Caddy hop) before a fail-closed check can be written. | Audit 2026-08-25 (tmux-aware launcher) | Defer | — | — |
| 27 | SEV-4 | Open | harness-frame (New Session dialog) | New Session → Start with zero ready machines closes the dialog silently (no labeled error state, CC1); a chat tab is labelled with the option *value* (`local_alden1`) rather than its display text. Pre-existing. | Audit 2026-08-25 (tmux-aware launcher) | Defer | — | — |
| 28 | SEV-4 | Open | harness-frame (tab shell) | Tab pattern is half-implemented for assistive tech: `role=tab` + `aria-selected` exist but panels lack `role=tabpanel` / `aria-controls` / `aria-labelledby`, and there is no roving `tabindex` / arrow-key handling. Pre-existing. | Audit 2026-08-25 (tmux-aware launcher) | Defer | — | — |
| 29 | SEV-4 | Open | control-plane (http headers) | No `Content-Security-Policy` / `frame-ancestors` on HTML responses (the long-tracked residual from the harness-frame audits). The frame ships an inline `<script>` and an inline `onclick`, which must move to a served asset first. `X-Content-Type-Options: nosniff` landed 2026-08-25 on every response. | Audit 2026-08-25 (tmux-aware launcher) | Defer | — | — |
| 30 | SEV-4 | Open | config-page | `CONFIG_CLIENT_JS.onEdit` builds a row with `tr.innerHTML` from `data-*` values through a local `esc()` that escapes `&` `"` `<` but not `>` or `'`. Every interpolation sits in a double-quoted attribute, so no break-out was found — but it is the exact sink the doctrine bans, guarded by a non-standard escaper. Pre-existing. Fix: `createElement`/`textContent`/`el.value`. | Audit 2026-08-25 (tmux-aware launcher) | Defer | — | — |
| 31 | SEV-3 | Open | deploy (pantheon-admin service) | **`systemctl restart` makes the OLD process abort (SIGABRT) during teardown; systemd counts it as a failure and auto-restarts it 3 s later.** Characterised 2026-08-25 during the tmux-launcher deploy — the journal shows `Statement::~Statement()` in `better-sqlite3` on the way out (`Main process exited, code=killed, status=6/ABRT` → `Scheduled restart job`), `NRestarts` increments once per deploy, and the service is healthy again ~3 s later. This is the "16-restart better-sqlite3 abort" watch item from the M1 plan §0.2. Likely fix: a graceful SIGTERM handler that closes the SQLite handle(s) (`app.close()` + `db.close()`) before exit. | Live verify 2026-08-25 | Defer | — | — |
| 32 | SEV-2 | Fixed | tmux-aware-launcher | **Attaching to an EXISTING tmux session failed on the Mac (`zsh:1: 0 not found`).** The exact-match target `-t =<name>` was unquoted; zsh — the Mac's login shell, through which sshd runs the remote command — treats `=word` as "expand to the path of command *word*" and aborts the line. Found by the live WebSocket attach check on VM 1093 minutes after deploy (attach-or-create, which has no `=`, worked). Fix: single-quote the target and the new-session name (`-t '=0'`, `-s 'solo'`); the allow-list admits no quote characters, so the quoting is literal under sh/bash/zsh. Verified `zsh -c "tmux has-session -t '=0'"` → exit 0 (unquoted → exit 1). | Live verify 2026-08-25 | Fix Now | `fix: quote tmux attach target` | Live 2026-08-25 |
| 33 | SEV-3 | Fixed | terminal-websocket-bridge / tmux-aware-launcher | **Closing a tab left the SSH session — and therefore a ghost tmux client — alive on the dev machine indefinitely.** By the Feature 3 design a dropped WebSocket only *detaches* (reconnectable), and the client had no way to say "I am closing on purpose", so every closed tmux tab kept a tmux client attached (visible in `tmux list-clients`, and it constrains the session's window size to the smallest client). Seen live 2026-08-25: session `0` showed `attached:1` after the probe tab closed. Fix: the client sends an explicit `{t:"c"}` frame on tab close; the bridge ends the SSH session (registry evicts); a bare socket drop still only detaches — the ruled "closing the tab equals detach" (topology doc §4) now holds: the tmux *session* keeps running, the *client* goes away. | Live verify 2026-08-25 | Fix Now | `fix: end the SSH session on explicit tab close` | Live 2026-08-25 |

<!--
  Severity: SEV-1, SEV-2, SEV-3, SEV-4 (see PROJECT_BIBLE.md Bug Severity Classification)
  Status: Open, Fixed, Deferred, Won't Fix, Post-MVP, Removed
  Disposition: Fix Now, Defer, Won't Fix, Post-MVP (assigned during triage, Step 2.8)
  Session: UAT session number where the bug was found (e.g., "Session 4")
  Fix Reference: PR number or commit hash of the fix (e.g., "PR #12" or "abc1234")
  Verified In: UAT session number where the fix was verified (e.g., "Session 5")
-->

## Status Guide

| Status | Meaning |
|---|---|
| **Open** | Bug confirmed, not yet fixed |
| **Fixed** | Fix implemented and verified |
| **Deferred** | Tracked with justification — must be resolved or feature removed at Phase 2→3 gate |
| **Won't Fix** | Accepted as-is with documented rationale (SEV-3/4 only) |
| **Post-MVP** | Moved to post-MVP backlog (SEV-4 enhancements only) |
| **Removed** | Feature containing the bug was removed |

## Severity Guide

| Severity | Definition | Examples | Can Defer? |
|---|---|---|---|
| **SEV-1** | Data loss, security breach, app crash on core flow | Auth bypass, database corruption, crash on login | No — must fix immediately |
| **SEV-2** | Feature broken but workaround exists, significant UX failure | Form submits wrong data, layout broken on one platform | Yes — but must resolve or remove feature at Phase 2→3 gate |
| **SEV-3** | Minor UX issue, cosmetic, non-core edge case | Alignment off, tooltip truncated, rare edge case | Yes |
| **SEV-4** | Enhancement, suggestion, polish | "Would be nice if...", performance optimization | Automatic Post-MVP |
